import 'reflect-metadata';
import 'dotenv/config';

// This file ensures reflect-metadata and dotenv are loaded
// before any test files. Jest's `rootDir` is set to `test`,
// so we reference this file via `<rootDir>/../jest.setup.ts`.
